# 转义

本页说明字符串输入中的**转义规则**。

## 适用范围
仅对**字符串输入端口**生效（例如：消息、命令、URL、字符串节点等）。  
数值、布尔、列表等非字符串端口不会触发解析。

## 转义规则
- `\$`：输出字面 `$`，不触发变量解析
- `\n`：输出换行
- `\\`：输出字面 `\`

示例：
- 输入：`\$bp('name')`
- 输出：`$bp('name')`
